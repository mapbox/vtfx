{
    "drop": {
        "name": "Drop",
        "description": "Drop a specified number of features from data.",
        "options": [
            {
                "field": "limit",
                "type": "number"
            }
        ], 
        "chainable": false
        },
    "labelgrid": {
        "name": "Snap features to grid",
        "description": "Snaps features to grid of specified size (in px), features to preserve are prioritized by order of input field.",
        "options": [
            {
                "field": "size",
                "type": "number"
            }
        ],
        "chainable": false
    },
    "orderby": {
        "name": "Order by feature",
        "description": "Returns sorted layer features based on which field to order by and sort direction.",
        "options": [
            {
                "field": "field",
                "type": "string"
            },
            {
                "field": "sort",
                "type": "number",
                "options": [{ "1": "asc"}, {"-1": "desc" }],
                "default": 1
            }
        ],
        "chainable": false
    }
}